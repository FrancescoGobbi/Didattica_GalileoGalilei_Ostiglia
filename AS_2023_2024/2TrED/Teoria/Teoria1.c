#include <stdio.h>

int main() {
    int a = 5; // Variabile intera
    float b = 3.5; // Variabile float
    char c = 'c'; // Variabile char

    // Stampo le variabili
    // Lo \n è usato per andare a capo riga
    printf("%d\n", a);
    printf("%f\n", b);
    printf("%c\n", c);
}