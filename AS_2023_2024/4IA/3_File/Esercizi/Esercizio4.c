#include <stdio.h>

#define DIM 15

int main() {
    // Nome del file
    char file_name[] = "scrittura.txt";
    // Variabili
    int val;
    int n;

    // Apertura del file
    FILE * fpIn = fopen(file_name, "wt");

    // Esercizio 2
    /*
    Leggere il file "valori.txt" e trasformare ogni parola presente nel file completamente in maiuscolo.
    Quindi ogni parola presente nel file (con lettere grandi o piccole che sia) deve essere portata completamente
    in maiuscolo

    ES: Casa --> CASA
    */

}