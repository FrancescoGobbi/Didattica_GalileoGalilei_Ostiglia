// Importiamo la libreria standard per input e output
#include <stdio.h>

// Definiamo la funzione principale del programma
int main() {
    // Dichiarazione di due variabili di tipo intero (4 byte)
    int x = 5;
    int y = 6;

    printf("Il primo numero è %d, il secondo numero è %d\n", x, y);

    // Dichiarazione di due variabili di tipo intero (4 byte)
    int c = 5, d = 6;

    printf("Il primo numero è %d, il secondo numero è %d\n", c, d);
}