// Importiamo la libreria standard per input e output
#include <stdio.h>

// Definiamo la funzione principale del programma
int main() {
    // printf() --> Funzione per la stampa a schermo (dare in output all'utente)
    // Messaggio tra doppie virgolette --> Messaggio che si vuole stampare
    // \n --> Utilizzato per andare a capo riga
    printf("Ciao Mondo!\n");
}